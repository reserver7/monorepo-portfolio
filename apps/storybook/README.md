# @repo/storybook

`@repo/ui` 디자인 시스템을 검증하는 Storybook 앱입니다.

## 실행

```bash
pnpm --filter @repo/storybook dev
pnpm --filter @repo/storybook build
```

## 구조

- 설정: `apps/storybook/.storybook`
- 스토리 소스: `packages/ui/stories`
