# Express + Socket.io server
PORT=4000

# Comma separated origins for CORS. Leave empty to allow all in local dev.
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Optional absolute path override for persisted state file
# STATE_FILE_PATH=/Users/you/project/apps/server/data/state.json
